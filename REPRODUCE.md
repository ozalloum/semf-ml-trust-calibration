# Reproduction

Install the dependencies listed in `code/requirements.txt`. Run commands from the repository root. The acquisition and validation workflow is in `code/stage3b/`; the locked analyses and figure generation are in `code/analysis/`. The release manifest, frozen protocol, and audit records document the required order and completion status.

Because the protocol is chronology-locked, do not retune parameters or replace the locked inputs when reproducing the reported results.

